import { SafeAreaView, Text } from "react-native";

const LobbyScreen = () => {
    return (
        <SafeAreaView>
            <Text>Lobby Screen</Text>
        </SafeAreaView>
    );
};

export default LobbyScreen;
